--- src/main/cpp/monerujo.cpp.orig
+++ src/main/cpp/monerujo.cpp
@@ -16,6 +16,7 @@
  */
 
 #include "wallet2_api.h"
+#include <cassert>
 
 using namespace std;
 
@@ -1128,12 +1129,14 @@
     return reinterpret_cast<jlong>(&wallet->txs);
 }
 
+/* Coins API removed in newer Monero versions - comment out
 JNIEXPORT jlong JNICALL
 Java_com_bitchat_android_monero_wallet_Wallet_coins(JNIEnv *env, jobject instance) {
     Monero::Wallet *wallet = getHandle<Monero::Wallet>(env, instance);
-    return reinterpret_cast<jlong>(wallet->coins());
+    return 0; // wallet->coins() removed in newer versions
 }
+*/
 
 JNIEXPORT jstring JNICALL
 Java_com_bitchat_android_monero_wallet_Wallet_getSeed(JNIEnv *env, jobject instance) {
@@ -1371,6 +1374,7 @@
     return reinterpret_cast<jlong>(tx);
 }
 
+/* CoinsInfo API removed - comment out
 jobject newCoinsInfo(JNIEnv *env, Monero::CoinsInfo *info) {
     jclass clazz = env->FindClass("com/bitchat/android/monero/model/CoinsInfo");
     jmethodID constructor = env->GetMethodID(clazz, "<init>", "()V");
@@ -1427,6 +1431,7 @@
     vector->clear();
     return reinterpret_cast<jlong>(vector);
 }
+*/
 
 JNIEXPORT jstring JNICALL
 Java_com_bitchat_android_monero_wallet_Wallet_getPublicViewKey(JNIEnv *env, jobject instance) {
